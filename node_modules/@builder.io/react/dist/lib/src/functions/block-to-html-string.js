"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.htmlEscape = function (str) {
    return str.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
};
// TODO: handle self closing tags
// TODO: how allow components (e.g. react components) in templates?
exports.blockToHtmlString = function (block) {
    var _a;
    return ("<" + exports.htmlEscape(block.tagName || 'div') + " \n    class=\"builder-block " + block.id + " " + (block.class || '') + "\"\n    builder-id=\"" + block.id + "\"\n  " + Object.keys(block.properties || {})
        .map(function (key) { return exports.htmlEscape(key) + "=\"" + exports.htmlEscape(block.properties[key]) + "\""; })
        .join(' ') + "\n  >" + (((_a = block === null || block === void 0 ? void 0 : block.component) === null || _a === void 0 ? void 0 : _a.name) === 'Text'
        ? block.component.options.text
        : block.children
            ? block.children.map(function (item) { return exports.blockToHtmlString(item); }).join('')
            : '') + "</" + (block.tagName || 'div') + ">").replace(/\s+/g, ' ');
};
//# sourceMappingURL=block-to-html-string.js.map